package graphics;

import java.awt.Canvas;
import java.awt.Color;
import java.awt.Graphics;
import java.awt.Toolkit;
import java.awt.image.BufferStrategy;
import java.awt.image.BufferedImage;
import java.awt.image.DataBufferInt;

import core.Loop;

public class PixelCanvas extends Canvas {

	private static final long serialVersionUID = 3186557580516093308L;
	private EpicDisplay display;
	
	public PixelCanvas() {
		DisplayManager.setScale(10);
		display = new EpicDisplay(DisplayManager.getWidth(), DisplayManager.getHeight());
		Toolkit.getDefaultToolkit().setDynamicLayout(false);
	}
	
	
	public void render() {
	
		BufferStrategy bs = this.getBufferStrategy();
		
		if (bs == null) {
			this.createBufferStrategy(3);
			this.requestFocus();
			return;
		}
		
		Graphics g = bs.getDrawGraphics();
		g.setColor(Color.BLACK);
		g.fillRect(0, 0, DisplayManager.getCanvasWidth(), DisplayManager.getCanvasHeight());
		
		display.render(g, Loop.getGame());
		
		g.dispose();
		bs.show();
	}
	
	@Override
	public void paint(Graphics g) {
		if (Loop.getGame() == null) {
			return;
		}
		DisplayManager.setCanvasWidth(Loop.getGame().getCanvas().getWidth());
		DisplayManager.setCanvasHeight(Loop.getGame().getCanvas().getHeight());
		System.out.println(DisplayManager.getFrameWidth() + " " + DisplayManager.getCanvasHeight());
	}
}
