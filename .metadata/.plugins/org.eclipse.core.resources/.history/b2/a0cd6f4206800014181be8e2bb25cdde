package graphics;

public class PixelDisplay extends PixelManager {

	private PixelManager test;
	
	public PixelDisplay(int w, int h) {
		super(w, h);
		
		test = new PixelManager(w, h);
		
		for (int i = 0; i < )
	}
	
}
