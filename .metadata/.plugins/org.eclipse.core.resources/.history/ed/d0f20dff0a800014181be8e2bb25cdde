package graphics;

import java.awt.image.BufferedImage;
import java.util.Random;

import core.Loop;

public class EpicDisplay extends PixelContainer {
	
	private PixelContainer container;
	private BufferedImage img;
	
	public EpicDisplay(int w, int h) {
		super(w, h);
		
		pixels = new PixelContainer(DisplayManager.getWidth(), DisplayManager.getHeight());
		
		Random rand = new Random();
		
		for (int i = 0; i < pixels.width * image.height; i++) {
			pixels.pixels[i] = rand.nextInt();
		}
	}
	
	public void render(Loop time) {
		formatPixels(pixels, 0, 0);
	}
}
