package graphics;

import java.awt.Graphics;
import java.util.Random;

import core.Loop;

public class EpicDisplay extends PixelContainer {
	
	private PixelContainer container;
	
	public EpicDisplay(int w, int h) {
		super(w, h);
		
		container = new PixelContainer(20, 20);
		
		
		Random rand = new Random();
		
		for (int i = 0; i < 20 * 20; i++) {
			container.pixels[i] = rand.nextInt();
		}
	}
	
	public void render(Graphics g, Loop time) {
		//int x = (DisplayManager.getFrameWidth() - img.getWidth()) / 2;
		//System.out.println(x);
		System.out.println("FRAME WIDTH: " + DisplayManager.getFrameWidth());
		formatPixels(container, (DisplayManager.getFrameWidth() - container.width) / 2, 0);
		System.out.println("FORMAT: " + (DisplayManager.getFrameWidth() - container.width) / 2);
	}
}
